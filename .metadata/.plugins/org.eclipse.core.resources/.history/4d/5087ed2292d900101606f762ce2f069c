package comm.library;

public class Book {
    /**
     *
     */
    private String title;
    /**
     *
     */
    private String author;
    /**
     *
     */
    private int yearPublished;
    /**
     *
     */
    private boolean isRented;
    /**
     *
     * @param title1
     * @param author
     * @param yearPublished
     */
    public Book(final String title1, String author, int yearPublished) {
        this.title = title1;
        this.author = author;
        this.yearPublished = yearPublished;
        this.isRented = false;
    }
    
    public boolean isRented() {
        return isRented;
    }
    
    public void rent() {
        if(!isRented) {
            isRented = true;
            System.out.println(title + " " + author + " " + yearPublished);
        }
    }
    
    public void display() {
        System.out.println(title + " " + author + " " + yearPublished); 
    }
}
